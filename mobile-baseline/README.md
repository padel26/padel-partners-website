# Mobile Baseline — 375px Screenshots

Last updated: 21 April 2026

These screenshots represent the locked mobile layout (375px width). Use these as a reference to ensure mobile styles are not affected by future desktop edits.

## Rule going forward
- Base (unprefixed) Tailwind classes = mobile — do not change without intention
- Desktop-only changes must use `lg:` or `xl:` prefixes only

## Pages captured (21 April 2026)
- Home (`/`) — hero, How We Work, packages, courts, services, newsletter ✅
- Packages (`/packages`) — all 6 cards, FAQs ✅
- Services (`/services`) — ServiceNav (3-row wrap on mobile), all categories ✅
- Courts (`/courts`) — header, 3 court cards, smart tech, build services ✅
- Trusted By (`/trusted-by`) — photo grid, who we work with, CTA ✅
- Contact (`/contact`) — form with all fields, book a call section ✅
- Blog (`/blog`) — hidden from nav, page intact and ready ✅

## Changes since v1 baseline (20 April 2026)
- Botpress chatbot bubble added (bottom right, all pages)
- PackagesExpand: Read more button moved to top-right, description full width
- Services: ServiceNav restored; Community & Coaching merged into one button on home
- SEO: JSON-LD schema, canonical tags, llms.txt, FAQ schema all added
- Blog hidden from nav and sitemap until posts are ready
- Trusted By title updated
